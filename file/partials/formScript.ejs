//controlling the "explore further" form：setting form values to previous user inputs retrieved from the database
var role = <%- JSON.stringify(role) %>;
var country = <%- JSON.stringify(country) %>;
$("#role").val(role);
$("#country").val(country);
